# Core dependencies
requests>=2.31.0
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Data processing
pandas>=2.1.0
numpy>=1.24.0

# Stock market data
yfinance>=0.2.32
alpha-vantage>=2.3.1

# Sentiment Analysis Options:
# Option 1: TextBlob (lightweight, no API needed, runs locally but simple)
textblob>=0.17.1

# Option 2: OpenAI API (best quality, requires API key and costs money)
openai>=1.3.0

# Option 3: Local ML models (OPTIONAL - only if you want FinBERT local model)
# Uncomment these lines if you want to run FinBERT locally:
# transformers>=4.35.0
# torch>=2.1.0

# Entity extraction (optional but recommended)
spacy>=3.7.0

# Data storage
sqlalchemy>=2.0.0
# Note: sqlite3 is built into Python's standard library

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0

# Utilities
python-dateutil>=2.8.2
pytz>=2023.3
tqdm>=4.66.0

# Testing and development
pytest>=7.4.0
black>=23.0.0
flake8>=6.1.0
